# REMANI-Planner 性能定位记录

## 已确认的根因

1. `fsm/global_plan=true` 时，`planning_horizon` 被强制设置为 `1000`。
2. `planner_manager.cpp` 中的无条件 `|| true` 使普通重规划也重复执行联合 A*；已移除。
3. 联合初始化的主要耗时在 `SampleMani` 机械臂层级搜索、`trajShot/oneShot` 后处理，以及 mesh 碰撞检查，不在 L-BFGS。
4. `SampleMani::search()` 之前没有实际使用 `max_loop_num`；现已同时受时间和循环次数限制。

## 实测记录

| 组别 | 配置 | 总规划时间 | 关键阶段 | 结果 |
|---|---|---:|---|---|
| A | 固定姿态，原始配置 | 约 0.18 s | KinoA* 12.4 ms，SampleMani 171 ms | 成功 |
| B | global_plan=false，horizon=3，loop=500，sample=0.05 | 25.844 s | SampleMani 25.828 s；底盘 A* 15.1 ms | 成功 |
| C | B + rewire=false | 11.766 s | SampleMani 8.035 s；whole-body RRT 3.390 s | 成功 |
| D | C + 0.05 m / 0.05 rad / 5 checks | 32.111 s | 初始化 27.077 s；最终安全检查 5.034 s | 成功，随机搜索波动明显 |
| E | D + oneShot=false 诊断 | 约 8.48 s | SampleMani 8.011 s；whole-body RRT 94.7 ms | 本次失败 |
| F | C + `max_mani_search_time=2.0`，横梁目标 `(1.5, 0)` | 2.485 s/次 | KinoA* 22.2 ms；SampleMani 2.037 s；fallback RRT 73.2 ms | 本次无路径 |
| G | 同一诊断实例第 2 次重试成功 | 29.672 s | SampleMani 搜索 2.082 s；`trajShot/oneShot` 23.263 s；最终安全检查 4.046 s | 成功 |
| H | C + 粗插值 + `max_oneshot_calls=4` | 2.690 s/次初始化 | SampleMani 搜索 2.012 s；oneShot 304.9 ms；计数 502 次碰撞检查 | 初始化成功，但后续优化未通过；3 次触发均未最终执行 |
| I | C + 粗插值 + `oneshot_stride=4` | 7.012 s 初始化；14.955 s 总规划 | SampleMani 搜索 2.138 s；oneShot 4.593 s；最终安全检查 4.000 s；818 次安全检查 | 成功，安全检查通过；执行 8.171 s |
| J | C + `oneshot_max_jump_layers=4` | 9.189 s 初始化；17.616 s 总规划 | SampleMani 搜索 2.035 s；oneShot 6.867 s；最终安全检查 4.292 s；816 次安全检查 | 成功，安全检查通过 |

计数示例：C 组 whole-body RRT 使用 9 个节点、24 次碰撞检查、0 次 rewire；D 组 SampleMani 为 1945 个节点、5158 次碰撞检查、4305 次边插值。

## 安全性

优化后轨迹发布前保留独立高精度完整检查，按 10 ms 采样调用完整 `MMConfig::checkcollision()`。已实测通过：811 个状态全部无碰撞；该检查耗时约 5.03 s，并计入端到端总时间。

## 当前结论

关闭 rewire 带来约 54% 的有效加速，但仍无法满足 2–5 s 首次规划目标。粗化 RRT 插值的单次结果受随机搜索影响，不能仅凭一次样本认定有效。当前新的主要瓶颈是 SampleMani 的搜索和后处理，以及最终高精度安全检查；L-BFGS 仍不是瓶颈。

F 组进一步确认：将机械臂搜索上限设为 2 秒后，单次初始化约 2.485 秒，但搜索失败会触发 FSM 的最多 3 次重试；因此一次用户触发动作的端到端等待时间仍可能接近 3 倍。该参数适合作为诊断或按场景选择，不能在未验证成功率前直接作为默认值。

G 组揭示了新的明确瓶颈：搜索本身只用了约 2.08 秒，但成功路径的 `trajShot/oneShot` 后处理用了约 23.26 秒，最终高精度安全检查用了约 4.05 秒。因此后续优化应优先针对 `oneShot` 的调用数量/碰撞检查，而不是继续优化 L-BFGS；最终安全检查仍然保留。

H 组表明简单截断 oneShot 虽可把初始化降到约 2.69 秒，但会改变 seed 质量：本次后续轨迹优化因加速度约束失败，不能直接作为默认策略。后续应改为“受预算限制但验证 shortcut 结果”的策略，或结合上一条可行轨迹 warm start，而不是无条件截断。

I 组表明按层稀疏执行 shortcut 比硬性截断调用次数更稳定：本次规划成功且高精度安全检查通过，但初始化仍为 7.012 秒。由于随机搜索和后端可行性检查仍有波动，`oneshot_stride` 当前仅作为诊断开关，默认值保持 1。

J 组中限制 shortcut 的祖先深度没有优于 I 组，反而达到 9.189 秒初始化；这表明当前随机树规模和碰撞检查数量的波动大于单一深度参数的收益，不能据此永久改变默认配置。

该成功样本的执行器日志显示轨迹执行时长 8.171 秒；末端跟踪误差多数小于 3 mm，最大观测约 8.4 mm，关节状态误差约 0.0064。该数据用于区分规划耗时与实际执行耗时。

所有诊断参数均可通过 launch 参数覆盖，默认配置不变。成功率记录为：A 1/1、B 2/2、C 1/1、D 1/1；E 本次 0/1。D 的另一轮曾因旧路径长度越界触发 SIGSEGV，已增加边界保护。
